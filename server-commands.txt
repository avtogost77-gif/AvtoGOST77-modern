# КОМАНДЫ ДЛЯ ВЫПОЛНЕНИЯ НА СЕРВЕРЕ

# 1. Перейти в директорию сайта
cd /www/wwwroot/avtogost77.ru/

# 2. Проверить что есть в корне
ls -la

# 3. Проверить наличие папок routes, industries, calculators
ls -la routes/ | head -10
ls -la industries/ | head -10
ls -la calculators/ | head -10

# 4. Найти генератор
find . -name "generate*" -type f

# 5. Проверить package.json (если есть)
cat package.json

# 6. Проверить GitHub Actions
ls -la .github/workflows/

# 7. Посмотреть sitemap
head -50 sitemap.xml

# 8. Подсчитать количество HTML файлов
find . -name "*.html" -type f | wc -l

# 9. Проверить последние изменения
ls -lt | head -20

# 10. Проверить git (если есть)
git status
git log --oneline -10