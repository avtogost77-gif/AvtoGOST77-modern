<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  <link rel="canonical" href="https://avtogost77.ru/{{ output_path }}">

  <!-- Open Graph -->
  <meta property="og:title" content="{{ title }}">
  <meta property="og:description" content="{{ description }}">
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://avtogost77.ru/{{ output_path }}">

  <!-- Schema.org -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Service",
    "name": "{{ title }}",
    "description": "{{ description }}",
    "provider": {"@type": "Organization", "name": "АвтоГОСТ"},
    "areaServed": [
      {"@type": "City", "name": "{{ from_city }}"},
      {"@type": "City", "name": "{{ to_city }}"}
    ],
    "serviceType": "{{ service_ru }}"
  }
  </script>

  <link rel="stylesheet" href="/assets/css/styles.css">
</head>
<body>
  <header class="header">
    <div class="container">
      <a href="/" class="logo">АвтоГОСТ</a>
      <nav class="nav">
        <a href="/services.html">Услуги</a>
        <a href="/blog/">Блог</a>
        <a href="/contact.html">Контакты</a>
      </nav>
    </div>
  </header>

  <main>
    <section class="hero-route">
      <div class="container">
        <div class="breadcrumbs">
          <a href="/">Главная</a> /
          <a href="/{{ service }}/">{{ service_ru }}</a> /
          <span>{{ from_city }} - {{ to_city }}</span>
        </div>
        <h1>{{ h1 }}</h1>
        <p class="hero-subtitle">{{ subtitle }}</p>
        
        <div class="hero-features">
          <div class="feature">
            <span class="icon">⚡</span>
            <span>{{ delivery_time }}</span>
          </div>
          <div class="feature">
            <span class="icon">📦</span>
            <span>{{ payload }}</span>
          </div>
          <div class="feature">
            <span class="icon">💰</span>
            <span>{{ price_benefit }}</span>
          </div>
        </div>
        
        <a href="#calculator" class="btn btn-primary btn-pulse">{{ cta_text or 'Рассчитать стоимость' }}</a>
      </div>
    </section>

    <!-- О маршруте -->
    <section class="route-info">
      <div class="container">
        <h2>{{ service_ru }} по маршруту {{ from_city }} — {{ to_city }}</h2>
        <div class="route-details">
          <div class="detail-card">
            <h3>Расстояние и время</h3>
            <p>{{ route_distance }} км • {{ route_time }}</p>
            <p>{{ route_description }}</p>
          </div>
          <div class="detail-card">
            <h3>Типы грузов</h3>
            <ul>
              {% for cargo in popular_cargo %}
              <li>{{ cargo }}</li>
              {% endfor %}
            </ul>
          </div>
          <div class="detail-card">
            <h3>Особенности маршрута</h3>
            <p>{{ route_features }}</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Преимущества -->
    <section class="advantages">
      <div class="container">
        <h2>Почему выбирают АвтоГОСТ</h2>
        <div class="advantages-grid">
          <div class="advantage">
            <span class="icon">🚚</span>
            <h3>{{ advantage_1_title }}</h3>
            <p>{{ advantage_1_text }}</p>
          </div>
          <div class="advantage">
            <span class="icon">💼</span>
            <h3>{{ advantage_2_title }}</h3>
            <p>{{ advantage_2_text }}</p>
          </div>
          <div class="advantage">
            <span class="icon">🛡️</span>
            <h3>{{ advantage_3_title }}</h3>
            <p>{{ advantage_3_text }}</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Калькулятор -->
    <section id="calculator" class="calculator-section">
      <div class="container">
        <h2>Рассчитайте стоимость {{ service_genitive }}</h2>
        <div class="calculator-form">
          <div class="form-row">
            <input type="text" id="from" placeholder="Откуда" value="{{ from_city }}">
            <input type="text" id="to" placeholder="Куда" value="{{ to_city }}">
          </div>
          <div class="form-row">
            <input type="number" placeholder="Вес (кг)" id="weight">
            <input type="number" placeholder="Объем (м³)" id="volume">
          </div>
          <button class="btn btn-primary" onclick="calculatePrice()">Рассчитать</button>
          <div id="result" class="result-block"></div>
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section class="faq-section">
      <div class="container">
        <h2>Частые вопросы о {{ service_prepositional }} {{ from_city }} — {{ to_city }}</h2>
        <div class="faq-list">
          {% for faq in route_faq %}
          <details class="faq-item">
            <summary>{{ faq.question }}</summary>
            <p>{{ faq.answer }}</p>
          </details>
          {% endfor %}
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="cta-section">
      <div class="container">
        <h2>{{ cta_title }}</h2>
        <p>{{ cta_subtitle }}</p>
        <div class="cta-buttons">
          <a href="tel:+74951234567" class="btn btn-primary">Позвонить</a>
          <a href="https://wa.me/74951234567" class="btn btn-secondary">WhatsApp</a>
        </div>
      </div>
    </section>

    <!-- Похожие маршруты -->
    <section class="related-routes">
      <div class="container">
        <h3>Похожие направления</h3>
        <div class="related-links">
          {% for route in related_routes %}
          <a href="/{{ route.url }}">{{ route.title }}</a>
          {% endfor %}
        </div>
      </div>
    </section>
  </main>

  <footer class="footer">
    <div class="container">
      <p>&copy; 2024 АвтоГОСТ. Все права защищены.</p>
    </div>
  </footer>

  <script src="/assets/js/main.js"></script>
  <!-- Yandex.Metrika -->
  <script type="text/javascript" >
    (function(m,e,t,r,i,k,a){ m[i]=m[i]||function(){ (m[i].a=m[i].a||[]).push(arguments)};
    m[i].l=1*new Date(); k=e.createElement(t), a=e.getElementsByTagName(t)[0]; k.async=1; k.src=r; a.parentNode.insertBefore(k,a)})
    (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");
    ym(103413788, "init", { clickmap:true, trackLinks:true, accurateTrackBounce:true, webvisor:true });
  </script>
  <noscript><div><img src="https://mc.yandex.ru/watch/103413788" style="position:absolute; left:-9999px;" alt="" /></div></noscript>
</body>
</html>